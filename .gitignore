# ── MEMBER DATA MUST NEVER ENTER GIT ─────────────────────────
*.db
*.db-journal
*.sqlite*
app.db*
CRM_Master*
*.csv
*.tsv
*.xlsx
*.docx
*.pptx
# ─────────────────────────────────────────────────────────────
__pycache__/
*.pyc
.env
.venv/
.DS_Store

# 7.8MB dashboard lives on the GCS data volume (/data/dashboard.html), not the image — upload to the bucket, never commit
dashboard.html

# ONAPGO report data/assets — live on the deploy bundle / GCS, never in git
onapgo_tiers.json
onapgo_assets/
sa-key.json
